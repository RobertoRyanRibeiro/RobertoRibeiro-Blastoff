using System;

namespace Parse
{
    internal class Program
    {
        static void Main(string[] args)
        {

            // (retorna um inteiro) Parse(string value);
            int inteiro = int.Parse("100");

            Console.WriteLine("Hello World!");
        }
    }
}
