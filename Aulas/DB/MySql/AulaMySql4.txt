DROP DATABASE IF EXISTS C207; #NÃO É UTILIZADO EM BANCO DE PRODUÇÃO
CREATE DATABASE C207;

CREATE TABLE alunos(
  matricula INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  curso VARCHAR(20),
  nome VARCHAR(45) NOT NULL,
  periodo INT  
);


CREATE TABLE alunos(
  matricula INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  curso VARCHAR(20),
  nome VARCHAR(45) NOT NULL,
  periodo INT  
) ENGINE = INNODB;

INSERT INTO alunos(curso,nome,periodo) VALUES ('GEC','FLAVIO',7);
SELECT * FROM alunos;
DELETE FROM alunos WHERE matricula = '1';
SELECT * FROM cursos
DROP TABLE cursos;

SHOW DATABASES;
SHOW TABLES;
SHOW TABLE STATUS;
DROP TABLE alunos;

CREATE USER 'FLAVIO' IDENTIFIED BY '1234';
SHOW GRANTS FOR FLAVIO;
DROP USER 'FLAVIO';

ALTER TABLE alunos ADD disciplina VARCHAR(50);
ALTER TABLE alunos DROP COLUMN disciplina;
ALTER TABLE alunos MODIFY disciplina INT;

INSERT INTO alunos(curso,nome,periodo) VALUES ('GEC','FLAVIO',7,'C207');

//--Outro Script--

USE C207;

SHOW TABLES;

CREATE TABLE cursos(
  id INT NOT NULL PRIMARY KEY
);
